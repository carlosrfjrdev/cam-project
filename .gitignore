# SDK ProfitDLL (Nelogica) — vendor binário/licenciado, NÃO commitar
# (DLLs Win32/Win64, PDFs do manual, exemplos, .zip). A integração read-only
# vive em apps/cam-cockpit/backend/cam/features/profit_bridge/.
/ProfitDLL/
/ProfitDLL.zip

# Relatórios de operações exportados (contêm conta/titular = PII) — NÃO commitar.
# Massa de trades p/ análise (trade_analyzer / motor estatístico) vive aqui.
/data/

# Manual NTSL em PDF (binário grande do vendor). A versão .md (texto) é versionada.
/project/trader-robots/ManualNTSL.pdf
