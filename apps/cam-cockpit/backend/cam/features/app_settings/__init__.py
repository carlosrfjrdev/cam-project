"""
app_settings — preferências de runtime do cockpit (persistidas em JSON).

Diferente de `_shared/config` (env/.env, imutável em runtime), aqui ficam escolhas
que o usuário altera pela tela de Configurações. Hoje: provedor de dados de mercado
(MT5 ativo / Profit em casca).
"""
