# Checkbox — DS CAM
**MUI:** `Checkbox`.
## Estados
off, on (fundo `--primary` + ✓ branco), indeterminate, disabled (opacity .4), focus (ring `--primary-soft`).
## A11y
`<input>` real escondido + label clicável; foco visível; teclado (space).
## CaM
Itens de checklist pré/pós-mercado (Arts. 32-33). Marcar não libera operação por si — Risk Engine governa.
