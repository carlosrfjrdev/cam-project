# Snackbar — DS CAM
**MUI:** `Snackbar` + `Alert` (era "Toast").
## Comportamento
top-right, ~4s, botão fechar, não empilhar demais. Borda esquerda colorida por severidade.
## Severidades
success/info/warning/error (tokens funcionais).
## Regra
Temporário — não substitui validação de form. Confirmação destrutiva = Dialog, não snackbar.
## CaM
Eventos: loop iniciado, análise IA pronta, DARF próxima, bridge offline (error — Order Gateway bloqueia).
