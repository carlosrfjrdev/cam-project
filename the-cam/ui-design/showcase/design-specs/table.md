# Table — DS CAM
**MUI:** `Table` (denso) ou `DataGrid` (avançado: sort/filter/virtualize).
## Anatomia
head (caption uppercase, muted) + rows (hover surface-2) + colunas numéricas alinhadas à direita em mono.
## Regras de defesa
Valores monetários SEMPRE líquidos (Art. 25º) — `PnlDisplay`. Ganho `--success`, perda `--destructive`. Status via chip (texto+cor).
## A11y
`<th scope=col>`, cabeçalho fixo opcional, ordenação anunciada.
## CaM
Decisões do Order Gateway, paper trades, holdings. REJECTED destacado; nunca exibir bruto.
