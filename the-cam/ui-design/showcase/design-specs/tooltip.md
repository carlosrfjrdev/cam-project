# Tooltip — DS CAM
**MUI:** `Tooltip`.
## Comportamento
aparece em hover/focus; fundo `--surface-2` + borda; ~240px; delay curto.
## A11y
`aria-describedby`; acessível por teclado (focus), não só hover.
## CaM
Explicar métricas técnicas (aderência, expectância, drawdown), ícones de status, flags constitucionais.
