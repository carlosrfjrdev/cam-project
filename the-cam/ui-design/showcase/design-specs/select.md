# Select — DS CAM
**MUI:** `Select` (era "Dropdown/SelectList").
## Anatomia
Trigger (valor + caret ▾) → menu de opções (surface-2, hover surface-3).
## Estados
default, focus (ring), open, disabled.
## A11y
`aria-expanded`, navegação por teclado, label associado.
## CaM
Selecionar estratégia ativa (só 1 — constraint DB), ambiente (paper/backtest/demo). Nunca "real" sem gate.
