# Pagination — DS CAM
**MUI:** `Pagination` / `TablePagination`.
## Anatomia
prev ‹ + páginas + ellipsis + next ›. Ativa = `--primary`.
## A11y
`aria-current=page` na ativa; botões com `aria-label`.
## CaM
Paginar journal, decisões, holdings, eventos de escalonamento.
