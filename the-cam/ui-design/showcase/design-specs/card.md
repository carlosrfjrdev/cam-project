# Card — DS CAM
**MUI:** `Card` / `Paper`.
## Anatomia
header (caption muted) + valor (mono) + footer. Sem sombra no dark (tonal layering); hover = borda `rgba(5,150,105,.4)`.
## CaM
Cards de métrica do cockpit: aderência, drawdown, líquido do dia. Valor numérico em mono; sinal com cor funcional + contexto.
