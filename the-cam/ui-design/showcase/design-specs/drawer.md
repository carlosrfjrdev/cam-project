# Drawer — DS CAM
**MUI:** `Drawer` (era "sidebar").
## Variantes
permanent (desktop, persistente), temporary (overlay rgba(0,0,0,.5), via hamburguer em telas estreitas).
## Anatomia
itens com ícone+label; ativo = borda+fundo esmeralda. Colapsável (208/56px).
## CaM
Navegação principal do cockpit (AppShell). Kill switch e banner de ambiente ficam no header, sempre visíveis.
